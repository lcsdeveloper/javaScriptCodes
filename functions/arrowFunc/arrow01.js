const names = [
	'lucas',
	'lisa',
	'maria',
	'aline'
];

console.log(names.map((names) => names.length));

//parentheses are optional if you have only one parameter name
const square = (n) => {
	return x * x;
};

//if there is no parameter you are able to put only an empty pair of parentheses
const show = () => console.log('Hi, Good Morning');
show();

function show(name)

function showName(name){
	console.log(name);
}

let lucas = 'lucas'
showName(lucas)

